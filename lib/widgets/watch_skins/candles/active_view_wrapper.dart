import 'package:flutter/material.dart';

class ActiveViewWrapper extends StatelessWidget {
  const ActiveViewWrapper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
