import 'package:flutter/material.dart';

class AppGap extends StatelessWidget {
  const AppGap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}
